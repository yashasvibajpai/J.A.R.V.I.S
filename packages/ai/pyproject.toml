[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis_ai"
version = "0.1.0"
description = "JARVIS AI Orchestration"
requires-python = ">=3.11"
dependencies = [
]
